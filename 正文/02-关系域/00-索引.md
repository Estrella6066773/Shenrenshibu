# 关系域索引

关系域用于回答"实体对象之间如何相互连接"的问题

## 关系类型

- 控制关系（control）：支配与被支配的关系
- 依赖关系（dependency）：相互依存的关系
- 冲突关系（conflict）：对立与矛盾的关系
- 交易关系（transaction）：交换与互惠的关系
- 渗透关系（infiltration）：渗透与影响的关系

## 录入规则

- 关系词条必须至少包含 `source`（来源）与 `target`（目标）两个要素
- 每条关系词条只描述一种主要关系类型，复合关系应拆分为多条独立记录
- 关系强度、时效性、证据来源等信息必须明确标注
