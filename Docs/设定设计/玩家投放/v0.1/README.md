# 玩家投放 v0.1

本版本为**首次结构化冻结**的对外口径包，对应内部设定母本的公开子集。考据者以本目录与 `manifest.yaml` 为**对外真值边界**；更深层的制度细节以内部 `真相/` 为准，本包不承诺完备披露。

## 包内文件

- `CHANGELOG.md`：相对「无冻结层」的初次建立说明。
- `manifest.yaml`：对外生效的 `id` 清单。
- `对外读本/`：面向玩家的导览与百科摘录文体。
