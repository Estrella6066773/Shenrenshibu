# 玩家投放 v0.1

> **已由 [v0.2](../v0.2/README.md) 取代**（2026-06-13）。本目录保留首次结构化冻结包供溯源。

本版本为**首次结构化冻结**的对外口径包，对应内部设定母本的公开子集。考据者以本目录与 `manifest.yaml` 为**该版本**对外口径边界；更深层的制度细节以内部 `真相/` 为准，本包不承诺完备披露。

## 包内文件

- `CHANGELOG.md`：相对「无冻结层」的初次建立说明。
- `manifest.yaml`：对外生效的 `id` 清单。
- `对外读本/`：面向玩家的导览与百科摘录文体。
