# 本地工程验收清单（Assets 不进 Git）

| 字段 | 值 |
|------|------|
| 状态 | 持续维护 |
| 最后更新 | 2026-06-29 |
| 最近验收 | **2026-06-29**（结构优化后静态验收 PASS；链检、预检、数据资产、日常委托场景） |

> `Assets/` 由 UVC 管理；下列结果来自仓库内实查，发版前可重跑文末命令复核。

## 1. 模块目录（module-glue-separation）

| 检查项 | 期望 | 结果 |
|--------|------|:----:|
| `Assets/01_Scripts/` 顶层目录 | `Assignment`、`ConflictSystem`、`Core`、`DialogSystem`、`Editor`、`Employee`、`IdleArea`、`MessageSystem`、`PlayerState`、`Portrait`、`Resume`、`StressSystem`、`Tests`、`UI` | **通过** |
| 与追溯矩阵主路径对齐 | 见 [交付-追溯矩阵](交付-追溯矩阵.md) | **通过** |
| `Tests/Runtime/` 仅测试辅助 | 不承载玩法真源 | **通过**（目录存在） |

## 2. Editor 工具链

| 检查项 | 期望 | 结果 |
|--------|------|:----:|
| `DisplayDialog` 零命中 | `rg "DisplayDialog" Assets --glob "*.cs"` | **通过**（2026-06-13 已改 `MailEventCsvToJsonConverter.cs` → `Debug.Log*`） |
| Unity 菜单前缀 `神人事部/` | `ShenRenShiBuAssetMenu.cs` 等 | **通过** |
| 编辑器创建路径 | 菜单默认写入 `Assets/04_Data/` | **通过**（`ShenRenShiBuAssetMenu.cs`、`CatQuestAssignmentTemplateMenu.cs`） |

## 3. Assets/00_Docs

| 检查项 | 期望 | 结果 |
|--------|------|:----:|
| 仅跳转 README → `Docs/README.md` | 无玩法真源副本 | **通过** |

## 4. 代码与 REQ 对齐（抽样）

| 检查项 | 期望 | 结果 |
|--------|------|:----:|
| REQ-MSG-005 聊天列表排序 | `ChatUIManager.SortSessionList` | **已实现**（2026-06-13） |
| REQ-ASG-011 日结钩子 | `PlayerEconomySystem.OnDayChanged` + `AssignmentDailyRefreshPublisher` | **已实现**；**已挂载**（`主场景`、`主场景_委托改进`） |
| `AssignmentBelongingKind.Daily` | 模板/数据字段已有 | **已实现**；`daily01`~`daily03` 已入注册表 |
| UAT 预检总表 | `verify-uat-preflight.py` | **通过** |
| UAT-115 对话 dat | `verify-uat-data-assets.py` | **通过** |
| UAT batch（11 项） | `run-uat036-tests.ps1` | **通过**（2026-06-14） |

## 5. 仍待人工（Play 模式）

| 项 | 说明 |
|----|------|
| Play 模式 UAT-012~035、020~035、040~114、116~126 | 须在 Unity 编辑器内逐项勾选 [交付-核心循环冒烟测试](交付-核心循环冒烟测试.md) |
| UAT-036 运行时 | `委托系统` 上勾选 `AssignmentDailyRefreshPlaymodeVerifier.enableVerifier` → Play → 等待日结跨天 |

## 6. 复核命令（PowerShell，项目根）

```powershell
Set-Location -LiteralPath 'D:\Unity\神人事部'
rg "DisplayDialog" Assets --glob "*.cs"
Get-ChildItem Assets\01_Scripts -Directory | Select-Object -ExpandProperty Name
python Docs/03-程序设计/05-交付/verify-assignment-daily-scene.py
python Docs/03-程序设计/05-交付/verify-uat-data-assets.py
python Docs/03-程序设计/05-交付/verify-uat-preflight.py
```

---

验收结果变更时更新上表 **结果** 列，并同步 [待细化追踪](../../00-规范/待细化追踪.md)。
