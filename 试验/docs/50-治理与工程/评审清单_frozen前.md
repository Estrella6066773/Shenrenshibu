# 评审清单：进入 frozen 前

词条状态拟从 `review` 升为 `frozen` 时，评审人须逐项确认下列项目。未通过项须记录整改责任人及截止日期。

## 结构与标识

- [ ] `id` 与 `ID注册表.md` 一致且无重复。
- [ ] `depends_on` 所列标识均已存在且路径正确。
- [ ] `contradicts` 已如实填写；存在张力时已在「争议与冲突」节或 `变更裁定记录.md` 中说明裁决。

## 真值与双维护

- [ ] 未在总览类词条（`LORE-RULE-002`）中重复展开应由其他宿主维护的长文。
- [ ] 观测域未发明新的身份、法律或组织设定。
- [ ] 术语与主编称谓与 `LORE-RULE-001` 及 `试验/docs/10-术语与命名/主编术语表.md` 一致。

## 可见性与对外影响

- [ ] 关键段落的 `[投放注记]` 完整或与「无差异省略」策略一致且无矛盾。
- [ ] `visibility.player` 与 `40-玩家投放包` 中相关版本清单无冲突。
- [ ] 已排除将 `LORE-RULE-003` 误作硬设定引用。

## 工程与协作

- [ ] 文件路径使用 `/`；无未解析的相对链接。
- [ ] Git 提交说明或评审单中可检索到本次变更动机与影响范围摘要。
