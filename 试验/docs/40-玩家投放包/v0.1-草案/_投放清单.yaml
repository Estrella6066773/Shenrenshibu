# 玩家投放包清单（示例草案）
# 说明：本文件为 YAML 语法注释采用 #；正式管线可改为 JSON 或由工具生成。

package:
  id: PLAYER-PACK-0.1-DRAFT
  name: 首测对外设定切片（草案）
  status: draft
  locale_default: zh-Hans
  effective_from: null
  owner: null

# 本包显式收录的设定标识（审计用）
lore_ids_included:
  - LORE-OBS-001
  - LORE-OBS-002
  # 按任务需要继续追加；禁止未登记即对外引用

# 明确排除（防误用）
lore_ids_excluded:
  - LORE-RULE-002
  - LORE-RULE-003

paths:
  common: 共通/对外背景摘要.md
  # 示例键；随实际文件增删

notes:
  - 迁移完成前，真值仍以 正文/ 下文件为准；本包句段须带 LORE 溯源。
