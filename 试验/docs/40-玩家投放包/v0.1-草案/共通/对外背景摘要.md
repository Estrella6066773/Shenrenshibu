# 对外背景摘要（占位）

## 状态

本文为**结构占位**，供明日会议或对接演示目录层级使用。正式发布前须由叙事负责人据 `LORE-TIME-003`、`LORE-ENT-ORG-001` 等宿主条撰写可朗读短文，并填入 `_投放清单.yaml` 之 `paths.common`。

## 撰写约束摘要

- 不得复述 `LORE-RULE-002` 全文结构；可使用其第三节呈现原则中的**已对外放行**表述。
- 涉及三群体差异时，须与 `LORE-OBS-001` 对齐，避免新增歧视性细节无溯源。
- 涉及「机器人管人」等营销语时，须附带边界说明（参见 `LORE-RULE-002` 相关表格与禁令）。

## 溯源块（发布时必填）

```yaml
sources:
  primary: []
  observation: []
  excluded_from_player_text:
    - LORE-RULE-002
    - LORE-RULE-003
```
