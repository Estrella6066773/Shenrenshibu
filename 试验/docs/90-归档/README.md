# 归档区

## 用途

存放已弃用（`deprecated`）的词条副本、合并前的历史版本快照或错误发布撤回稿。**不得**作为程序与对外素材的取数源。

## 迁入规则

- 迁入时须在文件首部注明 `status: deprecated`、替代标识与迁入日期。
- 原路径处应保留短重定向说明或 Git 历史中的移动记录，避免外链断裂。

## 当前状态

目录为空；待首次弃用词条时由维护人创建子文件夹并按 `ADR-*` 编号登记于 `试验/docs/50-治理与工程/变更裁定记录.md`。
